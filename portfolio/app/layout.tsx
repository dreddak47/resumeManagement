import type { Metadata } from "next";
import { Geist, Geist_Mono, Fraunces } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

const fraunces = Fraunces({
  variable: "--font-fraunces",
  subsets: ["latin"],
  axes: ["opsz", "SOFT", "WONK"],
});

export const metadata: Metadata = {
  title: "Aekansh Kathunia — Software Engineer",
  description:
    "Software engineer with an AI/ML lean — backend systems, RAG, reinforcement learning, and a handful of projects built at the intersection of engineering and personal interest.",
  metadataBase: new URL("https://aekansh.dev"),
  openGraph: {
    title: "Aekansh Kathunia — Software Engineer",
    description:
      "Backend systems, AI/ML, research, and competitive programming — plus a couple of projects built where engineering meets personal interest.",
    type: "website",
  },
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} ${fraunces.variable} h-full antialiased`}
      suppressHydrationWarning
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        {children}
      </body>
    </html>
  );
}
